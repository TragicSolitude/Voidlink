# Used for routing the custom url scheme to index.php while running locally.
# GAE automatically does all of this and ignores these .htaccess files.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^((?s).*)$ public/$1 [L]
</IfModule>
